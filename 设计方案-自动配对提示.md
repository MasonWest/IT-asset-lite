# 设计方案：主机 ↔ 显示器「自动匹配提示」

> **状态：提案待评审，未实现。**
> 这份文件不进入项目的三份正式文档（`CURRENT_STATE.md` / `CHANGELOG.md` / `README.md`）。
> 你拍板后我把结论并进 `CURRENT_STATE.md` 的「功能现状」与「下一步」两节，然后删掉本文件。
> 评估依据：`backend/data/it_assets.db` 只读扫描（143 台真实资产，2026-09-24）。

---

## 零、结论先行

**思路可行，但规则不能照抄。** 你的方向（只提示、不自动执行）是对的，这三条要改：

| | 你的思路 | 实测问题 | 改成 |
| --- | --- | --- | --- |
| 1 | 用「使用人 + 类型 + 位置」判断 | `user_name` 里 19% 不是人名 | 使用人只做**兜底**，不是主依据 |
| 2 | 位置相同或为空就提示 | 位置区分度为 0（45 个人里只有 1 人有 2 个位置） | 位置只做**排序权重**，不做过滤 |
| 3 | 「是否将当前显示器**与它**匹配」 | 张翔 2 主机 2 显示器、魏小茗 2 主机 1 显示器 | 必须给**候选列表**，不能自动挑一台 |

**还有一个你漏了的更强规则**：编号后缀。`B022-03-00001-M` 去掉 `-M` 就是主机编号——这条规则覆盖 92% 的显示器、**零歧义**、且和人工结果 46/47 一致。使用人规则只能在剩下的 8% 上补 2 台。

**所以规则优先级应该是：编号后缀 → 使用人 → （不用位置）。**

---

## 一、数据体检（凭什么这么说）

### 1.1 基本盘

```
资产总数        143 台
├─ host  类      67 台（主机 60 + 笔记本 7）
├─ display 类    50 台
└─ other 类      26 台（键盘/鼠标/打印机/路由器/扫描仪/电视/一体机/家具/装饰）
已配对显示器     47 台   未配对 3 台
```

### 1.2 两套规则的战斗力对比

| 规则 | 覆盖率 | 歧义 | 与人工结果一致 |
| --- | --- | --- | --- |
| **A：编号后缀** `xxx-M → xxx` | **46 / 50 = 92%** | **零**（后缀唯一，不可能命中两台） | 46 / 47 |
| **B：使用人相同** | 47 / 50（含 A 已覆盖的） | **3 / 50 无法唯一确定** | 46 / 47（使用人一致） |

剩下 4 台没有 `-M` 后缀的显示器，两套规则分别给什么：

| 显示器 | 使用人 | 规则 A | 规则 B |
| --- | --- | --- | --- |
| `B022-03-00073` | 洽谈室一 | 不适用 | 无候选 |
| `B022-03-00228` | 韩育涛 | 不适用 | `B022-03-00238` ✔ |
| `B022-03-00239` | 杜娜娜 | 不适用 | `B022-03-00022` ✔ |
| `B022-03-00163` | 张翔 | 不适用 | `B022-03-00003` ⚠ 已挂 2 台 |

→ **A 覆盖 92%、零误报；B 在剩余 8% 里能捞回 2 台。两者是互补的，不是二选一。**

### 1.3 真实库里「能提示但没配」的组合只有 2 组

全库扫描「主机没显示器 + 该主机使用人名下有显示器」的结果：

```
共 20 台主机没配显示器，其中只有 2 台的主机在「使用人名下有显示器」：
  B022-03-00231  [魏小茗]  → 名下显示器 1 台（且这人是 2 台主机，有歧义）
  B022-03-00238  [韩育涛]  → 名下显示器 1 台
```

**这个数字很重要：功能今天上线，存量只能捞回 2 台。** 它的价值不在存量，在**以后每录一台新设备时不漏配**。

### 1.4 status 维度很干净

- 已配对 47 条里，**没有一条**涉及报废 / 维修中的设备。
- 主机 `in_use` 54 / `idle` 13；显示器 `in_use` 49 / `idle` 1。
- 结论：不需要为 status 写复杂逻辑，**排除 `scrapped` 和 `repair` 双方**就够了。

---

## 二、三个硬伤（你那张表没考虑到）

### 硬伤 1：`user_name` 不是人名，是自由文本 —— 占 19% 的资产

`user_name` 刻意不做字典表（这是项目的设计决策，为了"随手输入就能用"），代价就是它会混进非人名：

| 值 | 台数 | 性质 | 会不会误配 |
| --- | --- | --- | --- |
| `/` | **15 台**（13 主机） | 占位符 | **会。**哪天录一台 `user_name='/'` 的显示器，会弹出 13 个候选 |
| `会议室二` | 3 台（2 主机） | 房间名 | 会。它不是"一个人"，是两台不同会议室的笔记本 |
| `会议室一` | 2 台（1 主机） | 房间名 | 同上 |
| `前台` | 2 台（1 主机 + 1 显示器） | 房间名 | 会"误报"——但其实是**对**的，前台主机确实该配前台显示器 |
| 空 | 2 台（1 主机 + 1 显示器） | 未填 | 会误报，这两台可能毫无关系 |
| `大厅` / `洽谈室一` / `公用（前台）` | 各 1 台 | 房间名 | 暂无风险 |

**受影响资产 27 / 143 = 19%。**

最危险的是 `/`：现在它名下有 13 台主机、0 台显示器，所以暂时不触发；**只要有人录一台 user_name 也写 `/` 的显示器，就是 13 选 1**。

→ **对应设计动作**：`user_name` 为空或纯符号的，**不出建议**；`user_name` 名下主机数 ≥ 3 的，降级为弱提示并显式警告。

### 硬伤 2：「同一人只有一台主机」不成立

```
张翔    : 主机 2 台 / 显示器 2 台     ← 2 选 1 是猜
魏小茗  : 主机 2 台 / 显示器 1 台     ← 而且其中 1 台是笔记本
'/':     主机 13 台 / 显示器 0 台
会议室二 : 主机 2 台 / 显示器 0 台
```

你原话是「是否将当前显示器**与其**匹配为一套台式机」——**"其"指哪台？** 系统不能替用户猜。这不是"要多写点提示文案"能解决的，是**交互形态**的问题：必须变成候选列表 + 用户单选。

### 硬伤 3：位置几乎不提供信息量

| 检查项 | 结果 |
| --- | --- |
| 45 个「既有主机又有显示器」的人里，名下位置 ≥ 2 个的 | **1 人** |
| 现有 47 条 active 配对中，位置一致的 | **47 条（100%）** |
| 现有 47 条 active 配对中，使用人一致的 | 46 条（差 1 条：刘宾 ↔ 殷金岭） |

→ 位置一致是**结果**不是**判据**（同一个人通常坐同一个工位，所以配对的两台位置自然一致）。把它当过滤条件，只会**漏掉**「换工位没改位置」这种真需要提示的场景。

**正确用法：位置只加权重，排候选顺序。同位置排前面，不同位置照常列出。**

---

## 三、对你那张表的逐行裁决

| # | 你的条件 | 裁决 | 理由 |
| --- | --- | --- | --- |
| 1 | 同一使用人 + 主机 + 显示器 + **同位置** → ✅ 强烈提示 | **采纳，但降档** | 实测位置不具有区分度（见硬伤 3）。降为「强提示」而非「强烈」，位置只影响排序 |
| 2 | 同上 + **位置为空** → ✅ 提示 | **采纳** | 当前库里空位置 0 台，几乎不触发。保留以防万一，成本为零 |
| 3 | 同上 + **位置不同** → ⚠️ 弱提示 | **采纳** | 这条对——"换了工位"正是需要人确认的场景。位置不同**不影响候选出不出**，只影响排序 |
| 4 | 不同使用人 → ❌ 不提示 | **采纳，但加例外** | 编号规则命中时**不看使用人**。库里有 1 条已配对是「刘宾主机 ↔ 殷金岭显示器」（人换了、使用人没同步）——编号规则能兜住这种 |
| 5 | 同一使用人 + 两台主机 → ❌ 不提示匹配 | **驳回** | 张翔、魏小茗就是这种情况，而他们的显示器**确实需要配**。不提示等于永远漏配。正确做法是**出候选让用户选**，而不是不提示 |
| 6 | 同一使用人 + 两台显示器 → ⚠️ 提示「副屏」 | **不需要新逻辑** | 现在数据模型就支持「一台主机挂 N 台显示器」，`asset_relations` 天生一对多。用户在任何一台显示器详情页都能直接挂到同一主机。「副屏」不是要提示，是要允许 —— 已经允许了 |
| 7 | 一体机 + 显示器 → ❌ 不提示 | **自动成立，零代码** | 库里 3 台一体机（`B022-03-00243/244/245`）的 `category` 是 **`other`**，不是 `host`，**天然不参与配对**，`pairing._ensure_pairable()` 会直接挡。你的规则不用写 |
| 8 | 笔记本 + 显示器 → ⚠️ 提示「外接显示器」 | **无法实现，建议放弃区分** | 笔记本的 `category` 是 **`host`**，和台式主机同档。要区分就得加 `category` 枚举值（`laptop`）——但**项目红线明确"不靠类型名判断、靠 category"**，加枚举就等于改数据模型。而且实测 7 台笔记本**全部 0 显示器**，这个需求从未发生。**建议：不区分，统一文案** |

---

## 四、设计方案

### 4.1 核心原则（三条，不可动摇）

1. **只建议，不执行。** 建议是 GET，只读，不写审计。用户点确认后才走已有的 `POST /api/pairings`（审计自动记 `ASSET_PAIR`）。配对是**业务事实**不是派生值，自动执行会污染审计与时间线。
2. **只做加法。** 候选里**只出现未配对的对手方**。已经挂在别处的显示器不列进候选（那需要"换绑"，是破坏性操作）——和 `link_monitors.py` 的口径一致。
3. **歧义必须交给用户。** 候选 ≥ 2 时永远让用户选，绝不按"第一台"或"最近创建"自动挑。

### 4.2 判定算法

**场景 A：录入一台显示器，找主机**

```
1. 编号规则
   if code 以 '-M' 结尾 且 code[:-2] 能在 host 类资产里找到
      → 强建议，候选唯一，score = 100，理由「编号后缀」

2. 否则走使用人
   2.1 user_name 为空、或纯符号（正则 ^[\/\-—\.\s]+$）→ 不出建议
   2.2 候选 = category = host 且 user_name 完全相同 且 status ∉ {scrapped, repair}
   2.3 排序：location 与当前资产相同的排前面（不加分也不减分，只排序）
   2.4 候选数 == 1 → 强建议 score = 60，理由「同一使用人」
       候选数 ≥ 2 → 弱建议，列出全部（最多 8 个，超出折叠），理由「同一使用人下有 N 台主机」
   2.5 候选数 == 0 → 不出建议
```

**场景 B：录入一台主机，找显示器**

```
1. 编号规则：存在 code == f"{host_code}-M" 的显示器 → 强建议
2. 否则：候选 = category = display 且 user_name 相同 且 **当前未配对** 且 status ∉ {scrapped, repair}
   排序同上（同 location 优先）
3. 额外只读提示（不进候选）：
   该使用人名下还有 N 台显示器已挂在别的主机上 → 显示为灰色说明文字，
   引导用户去「配对管理」用「换绑」，不在这里做
```

**明确排除**：`scrapped` / `repair` 的双方、自己配自己（已有校验）、`user_name` 为占位符的、`other` 类资产（一体机天然不在里面）。

### 4.3 触发点

| 触发点 | 做不做 | 理由 |
| --- | --- | --- |
| **① 新增资产保存成功后** | **做**（你要的） | 保存成功 → 有建议就弹非阻塞对话框 |
| **② 编辑资产改了使用人 / 位置后** | **建议做** | 最容易漏配的场景：先录设备、后填使用人。成本几乎为零（复用同一个判断） |
| **③ 批量导入结束后** | **做，但形态不同** | 导入是 N 行一起落库，**绝不能逐行弹窗**。在导入结果页加一段「有 12 台可以配对，去看看」，跳转到巡检页 |
| **④ 独立的「配对巡检」页** | **建议做，价值最高** | 一次性扫全库，列出所有"能配但没配"的组合。存量今天只有 2 组，但这是**唯一能兜住历史遗漏**的入口 |

① 是你提的；④ 才是真正解决问题的。建议 ①④ 一起做，②③ 次之。

### 4.4 接口设计

**新增两个只读接口**（挂在 `/api/pairings` 下，`relations.py`）：

```
GET  /api/pairings/suggestions?asset_id=123
     → 单台资产的建议。给「新增/编辑后」用。

GET  /api/pairings/suggestions?scope=all&limit=200
     → 全库巡检。给「配对巡检」页用。
```

返回结构（统一）：

```jsonc
{
  "asset": { "id": 123, "asset_code": "B022-03-00228", "category": "display",
             "user_name": "韩育涛", "location": "办公室大厅-运营部" },
  "has_suggestion": true,
  "confidence": "strong",            // strong | weak | none
  "reason": "user",                  // suffix | user | none
  "candidates": [
    {
      "asset": { "id": 88, "asset_code": "B022-03-00238", "brand": "联想",
                 "model": "ThinkCentre M720t", "user_name": "韩育涛",
                 "location": "办公室大厅-运营部", "status": "in_use",
                 "monitor_count": 0 },
      "score": 60,
      "evidence": ["同一使用人", "位置相同"]
    }
  ],
  "hints": [                          // 灰色说明，不是候选
    "该使用人名下还有 1 台显示器已挂在 B022-03-00099 上，可在「配对管理」换绑"
  ]
}
```

**注意**：`candidates[].asset` 复用列表接口的 `monitor_count` 口径，**不带 `host` / `monitors` 对象**（项目红线：列表不做 N+1，对端对象只在详情带）。

### 4.5 前端交互

| 情况 | 表现 |
| --- | --- |
| `confidence = none` | 什么都不显示，不打扰 |
| `confidence = strong`，候选 1 个 | 保存成功的 toast 里带一个「配一下」按钮 |
| `confidence = weak`，候选 ≥ 2 | 弹**非阻塞**对话框，Radio 列表，默认不选中，底部「暂不匹配」/「确认绑定」 |
| 全库巡检页 | 卡片列表，每张卡「主机 ← 显示器」+ 证据标签 + 「绑定」按钮，支持全选批量 —— **仍要用户点确认** |

新增的界面文案要写清**为什么**建议（「编号后缀一致」「同一使用人 · 位置相同」），不能只给个结论——否则用户没法判断该不该点。

### 4.6 不做什么

- ❌ 不做自动执行 / 批量静默配对（污染审计）
- ❌ 不引入「待确认建议」的持久化状态（`derive, don't persist` —— 建议是算出来的，不是存出来的）
- ❌ 不建唯一索引或新表
- ❌ 不动 `device_types.category` 枚举（不为"笔记本"加新值）
- ❌ 不改配对的现有校验和状态机（复用 `pairing.bind()`）
- ❌ 不做模糊匹配（`user_name` 相似度、拼音、错别字容错）—— 误配一次比漏配十次更贵

---

## 五、附带发现：建议先做一次数据清洗

这套功能上线前，下面这些值建议先清理（**独立于本功能**，是数据卫生问题）：

| 值 | 台数 | 建议 |
| --- | --- | --- |
| `user_name = '/'` | **15 台** | 改成空（`-` 写法见导入口径）或填真名。**不清掉的话这是最大的误配源** |
| `user_name = 会议室一 / 会议室二 / 大厅 / 前台 / 洽谈室一 / 公用（前台）` | 各 1–3 台 | 会议室类设备建议使用人留空、位置填房间名（位置字段本来就是干这个的） |

清洗后，使用人规则的可用性会显著提升。

---

## 六、工作量与分档建议

| 档位 | 内容 | 说明 |
| --- | --- | --- |
| **最小可用** | 场景 A 的编号规则 + 使用人规则、新增后提示、单候选 toast | 覆盖 92% + 2 台，解决你的原始诉求 |
| **推荐** | 上面 + 编辑后提示 + 独立巡检页 + `hints` 说明 | 增量不漏、存量能兜 |
| **完整** | 上面 + 导入后汇总提示 + 批量巡检确认 | 适合以后大批量录入时用 |

我的建议：**做「推荐」档**。最小可用档漏掉了历史遗漏（只解决新录入），而完整档的导入汇总在眼下用不上（140 台已经录完了）。

---

## 七、待你拍板

1. **规则优先级**采纳「编号后缀 > 使用人」吗？（这条是核心）
2. **一体机 / 笔记本**：同意「一体机自动不参与、笔记本不区分文案」吗？
3. **触发点**：做哪几档？（我建议「推荐」档：新增 + 编辑 + 独立巡检页）
4. **数据清洗**：`/` 那 15 台，先清还是先不管？
5. 是否需要**「弱提示也不弹窗」**的开关（有些用户嫌吵）？
