# IT 资产管理系统 —— 当前状态（CURRENT_STATE）

> **接手请先读这一份。** 读完这一份就够改代码了，其余文档都只是它的展开。
>
> | 项 | 值 |
> | --- | --- |
> | 版本基线 | **v1.1.1**（2026-09-24） |
> | 当前状态 | 三个阶段全部交付，自动化验收 **132 项 API + 79 项浏览器 + 36 项聚合视图 + 22 项配对优化**全绿 |
> | 技术形态 | 单体应用 · FastAPI + Vue3 · SQLite · 前端由后端托管 |
> | 启动方式 | 双击 `start.bat`（用**电脑上已装好的 Python**，不建虚拟环境） |
> | 数据库 | `backend/data/it_assets.db` |
>
> 相关文档：
> - [README.md](README.md) —— 面向使用者：怎么装、怎么跑、怎么用、常见问题
> - [CHANGELOG.md](CHANGELOG.md) —— 版本变更记录
> - `http://127.0.0.1:<端口>/docs` —— 参数级接口文档（由 FastAPI 自动生成）
>
> **原《每阶段总约束.txt》已逐字并入本文「一、项目约束」，原文件已删除**（见该节说明）。

---

## 一、项目约束（原《每阶段总约束.txt》）

> 以下是项目立项时定下的硬约束，**逐字保留，后续任何开发都不得违背**。
> 原文文件名：`每阶段总约束.txt`，已于 2026-09-24 并入本文并删除 —— 以后不需要再去翻那个文件。

```
这是一个轻量级 IT 资产管理系统，用于管理办公室的主机、显示器等设备。
技术约束：

单体应用，不要微服务，不要 K8s。

数据库用 SQLite，通过 ORM 访问，方便以后迁移。

后端推荐：Python FastAPI + SQLAlchemy；前端推荐：Vue3 + TypeScript + Vite + Element Plus。

前端打包后由后端托管，最终在 Windows 上能一条命令或双击脚本启动。

不要强制依赖 Docker，Docker 只能作为可选方案。

中文界面，风格现代简洁，参考飞书/Notion，不要传统后台那种密集表格感。

手机浏览器能正常打开、扫码、查看。

每阶段交付：源码 + README + 启动方式 + 验收步骤。

不要做采购、合同、财务、工单、审批流。
```

### 约束 → 落实对照

| 约束 | 怎么落实的 |
| --- | --- |
| 单体应用，不要微服务 / K8s | 一个 Python 进程同时提供 API 和前端静态文件，只有一个端口 |
| SQLite + ORM | SQLAlchemy 2.x + SQLite，没有裸 SQL 拼业务逻辑；换库只需改 `database.py` 的连接串 |
| FastAPI + SQLAlchemy / Vue3 + TS + Vite + Element Plus | 完全按推荐实现，未引入替代品 |
| 前端打包后由后端托管 | `frontend/dist` 由 FastAPI 的 `StaticFiles` 托管，`/` 直接出页面 |
| Windows 上一条命令或双击启动 | `start.bat` 双击即用；手动则是 `python run.py` 一条命令 |
| 不强制依赖 Docker | 仓库里没有 Dockerfile 也没有 compose，完全不涉及 |
| 中文界面 / 现代简洁 / 不密集 | 全中文；卡片 + 时间线式排版，没有传统后台那种密集表格堆砌 |
| 手机浏览器能打开、扫码、查看 | 全站响应式；每台设备出二维码，手机扫码直达详情页；手机端有盘点快捷条 |
| 每阶段交付源码 + README + 启动 + 验收 | 三个阶段均按此交付；验收结论见本文第十节，操作步骤见 README |
| 不做采购 / 合同 / 财务 / 工单 / 审批流 | 代码里完全没有相关模块、字段和路由 —— 这是**边界**，不是"还没做" |

---

## 二、这个系统是干什么的

办公室 IT 设备台账工具。流程一句话：

**录入 → 扫码看详情 → 主机显示器配对 → 状态流转 → 盘点 → 审计追溯 → 批量导入导出**

每台设备一个二维码贴在机器上；显示器挂在哪台主机上一页看清；领用/归还/送修/报废进时间线；
盘点手机扫码逐台标记；每次写操作（含失败的）都留审计；资产可 Excel 批量进出。手机浏览器直接用。

---

## 三、技术栈与运行环境

单体应用：FastAPI + SQLAlchemy 2.x + SQLite；前端 Vue 3 + TS + Vite + Element Plus，
构建产物由后端托管 —— 一个进程、一个端口。不依赖 Docker / Nginx / 微服务，
没有 Node 运行时依赖（只在构建前端时用 Node）。二维码用后端 `qrcode` 出图，表格用 `openpyxl`。

### 运行环境约定（重要）

**启动脚本使用「电脑上已经装好的 Python」，不创建虚拟环境、不自动装依赖。**

这是 2026-09-24 的明确决定：以前 `start.bat` 会自己建 `backend\.venv` 再往里装依赖，
在"本机 Python 本来就能跑"的机器上反而会因建环境/装包失败而起不来 —— 自找麻烦。

- 脚本按 `py -3` → `python` → `python3` 顺序**实测**，选第一个"能跑且依赖齐全"的
  （不是 `where python` 取第一个 —— 能找到一个 Python 和它能跑本系统是两回事）
- 缺依赖时只打印**缺哪几个包** + 一行可直接复制的安装命令，绝不自动往用户的 Python 里装东西
- 依赖自检交给 `backend/check_env.py`（只用标准库，任何 Python 都能跑它）
- 依赖只需**手动装一次**：`python -m pip install -r backend\requirements.txt`

---

## 四、目录结构

```
IT_snipe/
├── start.bat / stop.bat / backup.bat / dev.bat    ★ 启动 / 停止 / 备份 / 开发模式
├── README.md                 面向使用者的说明（怎么装、怎么用、FAQ）
├── CURRENT_STATE.md          ← 本文件：项目状态唯一入口
├── CHANGELOG.md              版本变更记录
├── .gitignore / .gitattributes
├── backups/                  备份文件（不入库）
├── .tmp/                     运行期临时文件，如 last-port.txt（不入库）
├── backend/
│   ├── run.py                启动入口
│   ├── check_env.py          依赖自检（标准库 only，给 .bat 探测用）
│   ├── backup_db.py          备份脚本（SQLite 在线备份接口 + 完整性校验）
│   ├── seed.py               手动灌演示数据（启动时不会自动灌）
│   ├── link_monitors.py      按编号后缀批量补主机↔显示器配对（默认干跑，--yes 才写）
│   ├── reset.py              清空业务数据（自动备份 + 输入 RESET 二次确认）
│   ├── requirements.txt
│   ├── data/it_assets.db     SQLite 数据库（首次启动自动建表 + 初始化设备类型字典）
│   └── app/
│       ├── main.py           应用装配 + 静态托管 + 审计中间件
│       ├── config.py         端口 / 局域网 IP / 扫码基准地址 / 备份目录 / 版本号
│       ├── database.py       引擎、会话、表分类、老库迁移（ensure_schema）
│       ├── models.py         七张表的模型定义
│       ├── schemas.py        请求响应模型
│       ├── seed.py           字典自动初始化（ensure_dictionary）+ 演示数据手动灌入（seed_demo）
│       ├── services/         业务逻辑（路由只管收发）
│       │   ├── events.py         变更事件 + 字段差异比较
│       │   ├── pairing.py        配对绑定 / 解绑 / 换绑
│       │   ├── serialize.py      模型 → 响应对象的统一出口
│       │   ├── filters.py        列表筛选与盘点范围共用的条件构造
│       │   ├── audit.py          审计写入（同事务 / 独立会话两条路径）
│       │   ├── spreadsheet.py    CSV / xlsx 导出与解析公共件
│       │   └── importer.py       批量导入：列定义 / 模板 / 预览 / 落库
│       └── routers/
│           ├── assets.py         资产 CRUD + 时间线 + 状态流转
│           ├── relations.py      配对管理
│           ├── inventory.py      盘点任务 / 标记 / 差异 / 导出
│           ├── device_types.py   设备类型管理
│           ├── transfer.py       批量导入 / 导出 / 模板（/api/transfer）
│           ├── audit.py          审计查询与导出（**只有 GET**）
│           ├── meta.py           筛选候选值 / 配对统计
│           └── system.py         系统信息 + 二维码出图
└── frontend/
    ├── vite.config.ts
    └── src/
        ├── App.vue            外壳：顶栏导航 + 设置弹窗（操作人 / 扫码地址 / 备份目录）
        ├── api/index.ts       接口封装
        ├── stores/app.ts      系统信息 & 扫码地址 & 当前操作人
        ├── utils/format.ts    图标 / 状态色 / 事件色 / 审计配色 / 日期格式化
        ├── components/        资产表单、类型管理、状态流转、配对面板、时间线、盘点条
        └── views/             台账列表、设备详情、配对管理、盘点列表/详情、
                               批量导入、操作审计、标签打印、系统设置
```

---

## 五、数据模型

七张表 + 一张迁移记账表：

| 表 | 存什么 |
| --- | --- |
| `device_types` | 设备类型 + 类别（`host` / `display` / `other`，决定能不能参与配对） |
| `assets` | 设备台账 |
| `asset_relations` | 主机 ↔ 显示器配对关系。**解绑不删行**，改成 `active = false` 并记录解绑人和时间 |
| `asset_events` | 变更事件 —— **设备视角**的时间线 |
| `inventory_tasks` | 盘点任务（范围、发起人、状态、结束时间） |
| `inventory_items` | 盘点任务里的每台设备 + 结果。带发起时的账面快照 |
| `audit_logs` | 操作审计 —— **操作者视角**。一次写操作一条，带操作人 / IP / 类型 / 对象 / 摘要 / 成败 / 明细 |
| `schema_meta` | 一次性迁移的记账表（key/value），只用来记「某个迁移已经做过了」 |

### 关于 `audit_logs` 和 `asset_events` 的分工

这是本项目最容易搞混的一处，**两张表不能合并**：

| | `asset_events` | `audit_logs` |
| --- | --- | --- |
| 视角 | 设备（挂在某台设备下，给那台设备的时间线看） | 操作者（一次用户操作一条） |
| 触发 | 设备自身发生了变化 | 有人做了一次写操作 |
| 失败操作 | 不记 | **记** |
| 批量导入 30 台 | 30 条（每台各一条变更） | **1 条**（"导入"这一件事） |

合并成一张表，两个视角都会瘸：要么时间线被"导入"这种批量操作刷屏，要么审计丢掉"谁试过但没干成"。

`audit_logs` 刻意**不挂外键**（`target_id` 就是个裸数字），也不跟随级联删除 —— 资产被删掉了，
「谁删的」这条记录还得在，不然审计就是自欺欺人。

---

## 六、工程红线（不可违背）

**1. 「改状态」和「记历史」必须在同一个事务里。**
所有变更走 `services/events.py` 的 `record_event()`，它只 `add` 不 `commit`，由调用方连同资产改动一起提交。
审计同理：`record_audit()` 也只 `add`。这样不会出现「状态改了但历史没记」的半截数据 —— 业务回滚，审计跟着回滚。

**2. 派生状态算出来，不落库。**
「这台主机挂了几台显示器」「盘点进度百分比」都是查询时现算的，不存表。一份数据两处存，迟早对不上。

**3. 时间一律存本地时间。**
`server_default=func.now()` 在 SQLite 下编译成 `CURRENT_TIMESTAMP`，**那是 UTC** —— 东八区机器上
下午 5 点录入的设备，库里写着上午 9 点。已全部改为 `datetime.now()`，老库做了一次性平移（见决策 13）。

**4. 审计日志只读。**
`routers/audit.py` 里只有 3 个 GET 端点，**没有任何写接口**。不提供编辑，也不提供删除。

**5. 不做登录体系。**
「操作人」由用户在设置里手填（可留空），靠 IP + 手填操作人区分。**不要自作主张加登录。**

**6. 启动流程只建表 + 补字典，绝不写业务数据。**
`lifespan` 里只允许调 `ensure_schema()` 和 `ensure_dictionary()`。想加"启动时顺便灌点什么"
之前先想清楚：删掉 `.db` 重启应该得到一个空库。演示数据只能由 `python seed.py` 显式触发。

**7. 新增/删除表要同步 `database.py` 的表分类。**
`BUSINESS_TABLES`（业务数据，`reset.py` 会清）和 `DICTIONARY_TABLES`（基础数据，自动补、不被清）
是人工维护的名单，`ASSET_COLUMNS` 那条红线同理 —— 漏一个就会让 `reset.py` 留下残缺数据。

---

## 七、关键设计决策（为什么这么做）

> 每条都是"看起来像 bug 的有意设计"。改代码前先看这里，否则很容易把有意的设计当 bug 改掉。

| # | 决策 | 理由 / 代价 |
| --- | --- | --- |
| 1 | 审计和变更历史是**两张表** | 见第五节的视角分工表，合并会让两个视角都残 |
| 2 | **失败的请求也写审计**，走独立会话 | 反复尝试删别人设备、非法状态被 409 挡下 —— 这些在业务表里毫无痕迹，恰是审计最值钱的信号。走独立会话是因为业务事务已经炸了，挂在上面写必然被回滚 |
| 3 | 审计 IP **不信任 `X-Forwarded-For`**，且用 **ContextVar** 传递 | 那个头能伪造，能自己填的 IP 让审计毫无意义（要设 `IT_ASSET_TRUST_PROXY=1` 才读）。用 ContextVar 是不想给十几个 handler 都加 `request` 参数 —— 它按异步任务隔离，不是全局变量，并发不串号 |
| 4 | 导入/导出/模板**共用同一份列定义**（`ASSET_COLUMNS`），且**预览与落库跑同一段代码**（`build_plan()`） | 三处同源就绝不会出现"导出的列导不回来"；同段代码就不存在"预览说没事、确认就报错"。也因此不用"服务端暂存 + token"那套（token 过期、文件换了、两人互相覆盖） |
| 5 | 更新已有设备时，**空单元格 = 不修改**，不是清空 | 批量改数据最常见的是"拿两列的表把 30 台机器的使用人刷一遍"。若空=清空，一次就抹掉品牌型号 SN 位置日期且不可撤销。想清空写 `-` |
| 6 | 导入结果分**新增 / 更新 / 无变化 / 失败**四桶，报错给 **Excel 真实行号** | 少了「无变化」，同一文件导第二遍会谎报"更新了 10 条"，用户再也无法判断导入是否生效。行号是给用户直接定位用的，给内部索引 0 等于没说 |
| 7 | 中文编码三处兜底：CSV 走 `utf-8-sig` → `gbk` → `latin-1`；下载文件名走 **RFC 5987** `filename*=UTF-8''` | 中文版 Excel「另存为 CSV」默认是 GBK，不认这个用户存一遍再传就全乱码。HTTP 头是 latin-1，中文文件名必须先 `quote`，同时留 ASCII `filename=` 给老浏览器兜底 |
| 8 | `/api/transfer` **独立前缀** | `/api/assets/{asset_id}` 是路径参数路由，`/api/assets/export` 会被当成 `asset_id="export"` 抢先匹配。靠调整路由顺序躲坑意味着以后每加一条都要重想 |
| 9 | 配对**解绑不删行**，改 `active=false` | 换绑轨迹才留得住 |
| 10 | 启动脚本**不建虚拟环境、不自动装依赖**，且**实测** Python 而不是取 PATH 第一个 | 见第三节。电脑上可能有好几个 Python，PATH 排第一的未必装了依赖；`py -3` 优先是因为它是官方启动器，不受 PATH 顺序影响 |
| 11 | 三个 `.bat` 一律 **GBK + CRLF、无 BOM** | Windows 控制台默认代码页 936。存成 UTF-8 中文提示会变乱码，配 `chcp 65001` 又会错位。`.gitattributes` 里用 `-text` 锁死，防止从 GitHub 直接下 ZIP 拿到 LF 版本 |
| 12 | **字典数据 ≠ 业务数据**，在 `database.py` 里分成两个常量组 | 设备类型是系统能跑的最小前提（没类型没法录资产），启动时自动补；`reset.py` 只清 `BUSINESS_TABLES`，不碰它，清完界面还能正常用 |
| 13 | **启动时不灌演示数据**，只自动初始化字典 | 自动灌会让人分不清"这些设备是我录的还是系统塞的"；更糟的是误删 `.db` 后重启会凭空长出十台假设备，跟真实数据混一起，比空库更难收拾。演示数据改成手动 `python seed.py` |
| 14 | 一次性迁移（补列 / 时区校正）**只平移** `created_at` / `updated_at`，并用 `schema_meta` **记账**保证幂等 | `released_at` / `closed_at` / `checked_at` 本来就是本地时间，往后挪 8 小时等于把好数据改坏。记账让重复启动不会重复执行 |
| 15 | `reset.py` **要求亲手输入 `RESET`**（不是 y/n），且**先确认、再备份、后清空**；删除顺序按**外键依赖**排（子表优先），不临时关外键校验 | 不可撤销操作，一个误敲的回车不该能触发它（`--yes` 留给自动化，`EOFError` 也当中止）。反过来先备份会在中止时留一堆垃圾文件；靠删除顺序而不是关校验，能让 schema 的问题在重置时就暴露出来 |
| 16 | 台账筛选里**设备类型 / 位置 / 使用人是多选，唯独「状态」保持单选** | 上面那排统计卡点一下就切状态，卡片本身是单选语义；状态也做成多选，就会出现"卡片只高亮一个、筛选框里却勾着三个"的错位 |
| 17 | 配对页主机列有「只看没配显示器的」，但待配对池的**「挂到主机」下拉故意不受它影响** | 那个下拉是用来挑**目标主机**的。跟着开关一起收窄的话，正想把显示器挂到某台主机上，却在下拉里找不到它 —— 两个控件的筛选范围必须各自独立 |
| 18 | 「加显示器」弹窗**默认只列还没配出去的显示器**，要看已挂机的得先勾一个开关 | 满屏「当前挂在 XXX（换绑）」是噪音，挑一台新显示器得先做完排除法。但换绑能力没有丢，勾一下就回来 —— 是"默认收起"不是"藏起来" |
| 19 | 多值筛选走**逗号分隔的字符串**（`1,3`），不用重复 query 参数（`?id=1&id=3`） | 列表页、「导出当前筛选结果」、盘点范围三处共用同一套 `build_conditions` 和同一个 `AssetQuery` 类型。逗号串让这三处一行都不用改；重复参数会逼着三处各自的参数拼装都改一遍，还容易只改对两处 |
| 20 | 套装聚合在**后端**做（`GET /api/assets/grouped`），不是前端把当前页数据分组 | 列表是 `id desc` + 每页 24 条，主机和它的显示器行距中位数 **85 行**、最大 131 行，47 条配对**没有一条落在同一页**。前端只拿得到当前页，连对手方都看不见 —— 勾了等于没聚合。分页必须在聚合**之后**做 |
| 21 | 组内**任意一台**命中筛选，整组完整出现（配套设备会被"顺带"带进结果） | 反例更糟：筛「类型 = 主机」时若只显示主机，那 47 台主机会显示成「未配显示器」—— 那是**错误信息**，比多显示一台更坏。代价是结果里混进了没被筛选命中的设备，所以页面上用一行蓝底提示把「命中 60 台 / 额外带出 47 台 / 共覆盖 107 台」写清楚，不让人以为筛选坏了 |
| 22 | 套装行**不参与**标签与导出逻辑，一律展开成设备明细 | 勾一个套装行打印，出的是 **2 张**标签不是 1 张；不说清楚用户会以为打少了。所以按钮文案直接带张数（「标签（2 张）」「打印选中标签（2 张）」）。导出更彻底：`currentQuery()` 里根本没有聚合开关，导出永远出明细，也永远不会因为列表换了视图而变样 |
| 23 | 台账页把两种模式统一成**同一种「行」结构**渲染 | 明细模式下每台设备被包成一个只有自己的 `single` 行。看着多一层包装，但换来模板 / 选中 / 标签 / 分页只有一套代码 —— 否则「两种视图 × 两种模式」= 4 套渲染分支，改一处必漏三处 |
| 24 | 配对页默认隐藏使用人写 `/` 的主机（半角 `/` 和全角 `／` 都认），**但这条规则只作用于主机侧，显示器一律不筛** | 项目没有人员字典表，使用人是自由文本，「这台不归谁」就被写成了斜杠 —— 15 台里 13 台是主机，全在公共区域，而且**一台显示器都没配**，正好全堆进「只看没配显示器的」：19 台里占 13 台，想挑出真正要配的那几台得先做一遍排除法。显示器不一样：写 `/` 的显示器照样得有人给它找主机去挂，藏起来只会漏配。两侧语义不同，就不该套同一条规则 |
| 25 | 过滤生效时，「只看没配显示器的（N）」的 N **在前端按过滤后的集合重算**，不用后端 `stats` | 后端那些数字是**全库统计**，不受页面开关影响。直接拿它当标签数字，会出现「写着 19 台、勾上只看得到 6 台」这种对不上的情况。同一页上「主机（54）」也是这个道理 —— 统计卡是全局事实，列表是筛选结果，两者本来就不该相等，但**相邻的两个控件不能互相矛盾** |

---

## 八、功能现状

三个阶段已全部交付，**现在有的能力**：资产台账（CRUD / 筛选 / 二维码 / 标签打印）·
主机-显示器配对（绑定 / 换绑 / 解绑，解绑留记录）· 状态流转（领用 / 归还 / 送修 / 修好 / 报废 / 恢复，
非法流转 409）· 盘点（发起 / 扫码标记 / 差异 / 导出）· 操作审计（18 种操作类型，失败也留痕，只读）·
批量导入导出（模板 → 预览四桶 → 入库，列与导出一致）·
台账**套装聚合视图**（把主机和它挂的显示器合并成一行看，只换看法不动数据）。

**筛选能力（v1.0.2 起）**：四个条件参数（`device_type_id` / `status` / `user_name` / `location`）
统一支持**逗号分隔多值**，列表页与「导出当前筛选结果」、盘点范围共用同一套 `build_conditions`。
界面上：台账的类型 / 位置 / 使用人为多选（状态保持单选，见第七节 #16）；
标签打印可按类型 + 位置缩范围；配对页可按关键字搜、可只看没配显示器的主机，
给主机加显示器时默认只列未配对的。

**配对页的「使用人写 `/` 的主机」默认隐藏（v1.1.1 起）**：页面顶部一个开关，默认勾上。
只作用于**主机侧**（主机列 + 待配对池的「挂到主机」下拉共用同一集合），显示器一律不筛；
开关文案直接带出台数，「只看没配显示器的」计数按过滤后重算。口径见第七节 #24、#25。

**设备类型图标（v1.1.1 起）**：映射表在 `frontend/src/utils/format.ts`，
类型名精确命中优先，其次按关键字兜底（自建「显示器（曲面）」「NAS」这类名字也能命中），
都没命中才是 `📦`。**改图标前先在真实 Chromium 里渲染一遍** —— Windows 上有几个看着很合适的
码点（`U+1F5B5` SCREEN、`U+1F5B4` HARD DISK、`U+1F5C5` 空文件柜）实际是豆腐块。

**套装聚合视图（v1.1.0 起）**：台账页筛选栏右侧的「套装聚合展示」开关。开启后走
`GET /api/assets/grouped`（筛选参数与明细接口完全一致，服务端按**聚合后的行**分页），
143 行 → 96 行。卡片视图显示套装卡 + 成员 chip，表格视图带展开明细。
口径见第七节 #20~#23；实现见 `backend/app/services/bundles.py` 开头的说明。
**它是只读的展示视角**：不写数据、不产生审计、不影响导出与标签（那两者永远走设备明细）。

> 逐项功能说明与每个功能的操作步骤 → [README.md](README.md)。

---

## 九、怎么跑

1. 装一次依赖：`python -m pip install -r backend\requirements.txt`
2. 双击 `start.bat`（挑端口 → 实测 Python → 查依赖 → 起服务 → 5 秒后开浏览器）
3. 停：窗口内 `Ctrl+C`，或双击 `stop.bat`；备份：双击 `backup.bat`
4. 不用脚本：`cd backend && python run.py`（换端口加 `--port 18080`）
5. 局域网 / 手机访问、防火墙、探测到的 IP 不对怎么办 → [README.md](README.md)
6. 数据维护命令（`seed.py` 灌演示 / `link_monitors.py` 批量配对 / `reset.py` 清空）→ [README.md](README.md) 的「三个维护命令」

---

## 十、验收

| 脚本 | 覆盖 | 结果 |
| --- | --- | --- |
| `phase3_api_test.py` | 后端 16 个 section / 132 项 | **132 / 132 通过** |
| `e2e-phase3.js` | 真实 Chromium 浏览器端到端 / 79 项 | **79 / 79 通过** |
| `e2e-v102.js` | v1.0.2 四个优化点 / 25 项（含手机视口） | **25 / 25 通过** |
| `e2e-v110.js` | v1.1.0 套装聚合视图 / 36 项（含手机视口与 mock 场景） | **36 / 36 通过** |
| `e2e-v111.js` | v1.1.1 配对页两处优化 / 22 项（含手机视口） | **22 / 22 通过** |
| `vue-tsc --noEmit` · `vite build` | 前端类型检查 / 构建 | 通过 |

> 前两个脚本是开发期工具，放在仓库之外的本地测试目录，**未随项目发布**。
> 它们每次运行前会把数据库复位成 10 台种子资产并清空审计记录，为的是让断言有确定起点 ——
> 所以**千万不要对着正在使用的数据跑**。

需要人工确认的 4 项：双击 `start.bat`、双击 `backup.bat`、手机连同一局域网扫码、删库重启是空库。
操作步骤分别在 [README.md](README.md) 的「快速开始」「日常操作」「局域网内其他设备怎么访问」「常见问题」四节。

新增的 `seed.py` / `reset.py` 已在命令行实测：删库重启后的空库状态、`seed.py` 的幂等跳过、
`reset.py` 的两条中止路径（空 stdin / 输错词）与成功路径（备份 + 删除 + VACUUM 回收）。

`link_monitors.py` 已在真实数据上实测（143 台资产 / 46 台 `-M` 显示器）：干跑计划 46 对，
`--yes` 备份后一次写入成功，重跑第二遍全部落到「已配好」分支、零重复写入；
写入后核对 `asset_relations` 47 条 active、`asset_events` 92 条 pair、审计 1 条，
并经 `/api/assets` 列表（`host_code` / `monitor_count`）与详情（`host` 对象）双向确认。

`e2e-v102.js` 走的是**另一条路**：**不复位数据**，直接对着真实库做只读验收 ——
每条断言都是「先打接口拿到真相数字，再读界面上显示的数字，两边对账」，
全程没有一次写操作，所以可以对着正在使用的库跑（前两个脚本则**绝对不行**）。
覆盖：台账多选逐项对账、配对页两处开关、加显示器弹窗的默认隐藏与开关放开、
标签页类型筛选与清空、标签页初始态无预选（防 ref 撞名）、390×844 手机视口无横向溢出、控制台无 JS 报错。

`e2e-v110.js` 沿用同一套打法，覆盖：开关默认关、聚合后行数 96 与接口一致、
卡片套装 + 成员 chip、表格套装行展开出显示器明细、勾选套装行的标签张数 = 成员台数、
筛「主机」时额外带出的 47 台与接口一致、导出弹窗写明「每台设备各占一行」、
刷新后偏好保持、390×844 两种视图都无横向溢出、控制台无 JS 报错。

其中 **`flags` 的「多显示器」与「状态不一致」在当前真实数据上都是 `false`**
（库里没有一台主机挂 2 台显示器）—— 这两条是用 `page.route` **拦截接口响应注入 mock 数据**
验证渲染的。这样才能测到"界面对尚未发生的数据"的处理，又不用往真实库里塞假设备。

`e2e-v111.js` 同样只读，覆盖：默认隐藏的条数与接口对账、开关文案带出被藏台数、
被藏设备的编号一个都不露、取消勾选后全部回来、
「只看没配显示器的」数字按过滤后算**且校验它不等于后端全局统计**、
「挂到主机」下拉与主机列范围一致、加显示器弹窗每条都有使用人标签且**逐条与接口 user_name 对账**、
位置也带上了、搜使用人仍能命中、390×844 手机视口无横向溢出、控制台无 JS 报错。

图标改版逐类型打开详情页读实际字符核对（主机 `🗄️` / 显示器 `🖥️` / 一体机 `🖳` /
电视 `📺` / 家具 `🪑` + 配对页左右两栏），并另写渲染对照页把「改前 vs 改后」和
候选码点一起画出来 —— **Windows 上有几个码点是豆腐块，只靠名字判断会踩坑**。

---

## 十一、接口一览

服务起来后打开 `http://127.0.0.1:<端口>/docs` 有交互式文档。按前缀分组：

| 前缀 | 覆盖 |
| --- | --- |
| `/api/assets` | 台账 CRUD、时间线、配对记录、状态流转；`/api/assets/grouped` = 套装聚合视图（只读） |
| `/api/pairings` | 配对看板、绑定、换绑、解绑、记录 |
| `/api/inventory` | 盘点任务、标记、差异、导出、扫码上下文 |
| `/api/device-types` | 设备类型增删改查 |
| `/api/transfer` | 模板下载、批量导入、批量导出 |
| `/api/audit` | 审计列表、筛选项、导出（**只有 GET**） |
| `/api/meta/filters` | 筛选候选值 + 配对统计 |
| `/api/qrcode.png` · `/api/system/info` · `/api/health` | 二维码出图、系统信息、健康检查 |

参数级定义以 `/docs` 为准，本文不重复维护。

---

## 十二、已知限制

- **不能用手机摄像头实时扫码**。浏览器要求 HTTPS 或 localhost 才能调摄像头，而本系统是局域网
  `http://192.168.x.x`。改用系统相机 / 微信扫已经贴好的二维码 —— 效果一样。
- **没有登录和权限**。这是有意的（见红线 5），「操作人」靠手填 + IP 区分，不能作为强身份证明。
- **没有附件**（发票、保修卡照片），没有保修到期提醒。
- **审计日志不能删**。数据量大时只能靠筛选和分页，没有归档/清理功能。
- **导入是同步的**。超大文件（上万行）会让请求变慢，目前没有后台任务队列。
- **`frontend/dist` 需要 Node 才能构建**。把 dist 一起拷过去，目标机器不装 Node 也能跑。

---

## 十三、下一步（尚未实现）

按价值排序：

1. **附件**：发票、保修卡照片，挂在设备下
2. **保修到期提醒**：`warranty_until` 字段已经在库里了，差一个提醒视图
3. **登录与权限**（如果确实需要 —— 但会打破「不做登录」的当前设计）
4. **审计归档**：按时间把老审计导出后清理
5. **主机 ↔ 显示器自动匹配提示**：已完成数据体检与方案设计，因存量收益低（仅 2 组可捞回）、
   规则复杂度高，**暂缓实现**。待资产录入量增大或使用人数据清洗完成后再评估。
   （体检结论与设计方案存档在根目录 `设计方案-自动配对提示.md`，未实现，重开时先读它。）

明确**不要做**的：采购、合同、财务、工单、审批流（立项约束）。

---

## 十四、维护约定

改代码后按这个顺序自查，别跳步：

1. `python backend\check_env.py` —— 环境没坏
2. `cd frontend && npx vue-tsc --noEmit` —— 类型干净
3. `npm run build` —— 前端能构建
4. 复位数据库后跑 `phase3_api_test.py` 与 `e2e-phase3.js` —— 自动化验收全绿

容易踩的坑（`bat` 编码、路由前缀顺序两条已升级为第七节的决策 11 / 8，此处不重复）：

- **改了后端依赖要同步改 `backend/check_env.py` 的清单**。`requirements.txt` 管"装什么"，
  `check_env.py` 管"装完能不能 import"，两边必须一起动。
- **改了表结构要同步 `database.py` 的 `BUSINESS_TABLES` / `DICTIONARY_TABLES`**，
  否则 `reset.py` 和 `seed.py --check` 会漏表。
- **改行为别改 `seed.py` 与 `main.py` 的分工**：字典走启动，演示数据走命令行。
- **按改动类型找对应文档**：改了用户可见行为 → 更新 [README.md](README.md)；改了设计决策 /
  数据模型 / 工程红线 → 更新本文件；只改文案、样式、内部重构 → 两者都不动。

---

*最后更新：2026-09-24 · 版本基线 v1.0.2*
