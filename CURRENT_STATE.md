# IT 资产管理系统 —— 当前状态（CURRENT_STATE）

> **接手请先读这一份。** 项目现在是什么样、为什么这么做、做到哪一步、下一步干什么，
> 这一份里全都有；其余文档只是它的展开。
>
> | 项 | 值 |
> | --- | --- |
> | 版本基线 | **v1.0.0**（2026-09-24 定版） |
> | 当前状态 | 三个阶段全部交付，自动化验收 **132 项 API + 79 项浏览器全绿** |
> | 技术形态 | 单体应用 · FastAPI + Vue3 · SQLite · 前端由后端托管 |
> | 启动方式 | 双击 `start.bat`（用**电脑上已装好的 Python**，不建虚拟环境） |
> | 数据库 | `backend/data/it_assets.db` |
>
> 相关文档：
> - [README.md](README.md) —— 面向使用者的完整说明（功能、操作、常见问题、详细验收步骤）
> - [CHANGELOG.md](CHANGELOG.md) —— 版本变更记录
>
> **原《每阶段总约束.txt》已逐字并入本文「一、项目约束」，原文件已删除**（见该节说明）。

---

## 一、项目约束（原《每阶段总约束.txt》）

> 以下是项目立项时定下的硬约束，**逐字保留，后续任何开发都不得违背**。
> 原文文件名：`每阶段总约束.txt`，已于 2026-09-24 并入本文并删除 —— 以后不需要再去翻那个文件。

```
这是一个轻量级 IT 资产管理系统，用于管理办公室的主机、显示器等设备。
技术约束：

单体应用，不要微服务，不要 K8s。

数据库用 SQLite，通过 ORM 访问，方便以后迁移。

后端推荐：Python FastAPI + SQLAlchemy；前端推荐：Vue3 + TypeScript + Vite + Element Plus。

前端打包后由后端托管，最终在 Windows 上能一条命令或双击脚本启动。

不要强制依赖 Docker，Docker 只能作为可选方案。

中文界面，风格现代简洁，参考飞书/Notion，不要传统后台那种密集表格感。

手机浏览器能正常打开、扫码、查看。

每阶段交付：源码 + README + 启动方式 + 验收步骤。

不要做采购、合同、财务、工单、审批流。
```

### 约束 → 落实对照

| 约束 | 怎么落实的 |
| --- | --- |
| 单体应用，不要微服务 / K8s | 一个 Python 进程同时提供 API 和前端静态文件，只有一个端口 |
| SQLite + ORM | SQLAlchemy 2.x + SQLite，没有任何裸 SQL 拼业务逻辑；换库只需改 `database.py` 的连接串 |
| FastAPI + SQLAlchemy / Vue3 + TS + Vite + Element Plus | 完全按推荐实现，前后端各一套依赖，未引入替代品 |
| 前端打包后由后端托管 | `frontend/dist` 由 FastAPI 的 `StaticFiles` 托管，`/` 直接出页面 |
| Windows 上一条命令或双击启动 | `start.bat` 双击即用；手动则是 `python run.py` 一条命令 |
| 不强制依赖 Docker | 仓库里没有 Dockerfile 也没有 compose，完全不涉及 |
| 中文界面 / 现代简洁 / 不密集 | 全中文；卡片 + 时间线式排版，没有传统后台那种密集表格堆砌 |
| 手机浏览器能打开、扫码、查看 | 全站响应式；每台设备出二维码，手机扫码直达详情页；手机端有盘点快捷条 |
| 每阶段交付源码 + README + 启动 + 验收 | 三个阶段均按此交付，验收步骤见 README「验收步骤」节 |
| 不做采购 / 合同 / 财务 / 工单 / 审批流 | 代码里完全没有相关模块、字段和路由 —— 这是**边界**，不是"还没做" |

---

## 二、这个系统是干什么的

办公室 IT 设备的台账工具。一句话概括流程：

**录入 → 扫码看详情 → 主机显示器配对 → 状态流转 → 盘点 → 审计追溯 → 批量导入导出**

- 每台设备一个二维码，贴在机器上，手机扫一下就能看到这台机器的全部信息
- 显示器挂在哪台主机上，一页看清、一步改完，换绑轨迹留得住
- 领用 / 归还 / 送修 / 报废都记进时间线，每台设备一条历史
- 盘点用手机扫码逐台标记，最后导出差异报表
- 每一次写操作都留审计（谁、什么时候、从哪个 IP、动了什么、改成什么），**失败的操作也留**
- 资产可以 Excel 批量导入导出，导出的文件改一改能直接导回来
- 批量打印二维码标签（A4 纸直接打，剪下来贴）
- 手机浏览器直接用，不装 App

---

## 三、技术栈与运行环境

| 层 | 选型 | 本机实际版本 |
| --- | --- | --- |
| 后端 | Python 3.10+ / FastAPI / SQLAlchemy 2.x | Python 3.13.2 · FastAPI 0.124.2 · SQLAlchemy 2.0.54 |
| 数据库 | SQLite（自动创建，无需安装） | — |
| 前端 | Vue 3 + TypeScript + Vite + Element Plus | Vue 3.5 · Vite 6 · Element Plus 2.9 |
| 二维码 | 后端 `qrcode` 生成 PNG | qrcode 8.2 |
| 表格 | 后端 `openpyxl` 读写 xlsx | openpyxl 3.1.5 |
| 部署 | 单体：前端构建产物由后端托管，一个进程一个端口 | — |

不依赖 Docker、不需要 Nginx、不需要微服务、没有 Node 运行时依赖（构建时才用 Node）。

### 运行环境约定（重要）

**启动脚本使用「电脑上已经装好的 Python」，不创建虚拟环境。**

这是 2026-09-24 的明确决定，原因是之前 `start.bat` 会自己建 `backend\.venv` 再往里装依赖，
在"本机 Python 本来就能跑"的机器上反而容易因为建环境/装包失败而启动不了 —— 属于自找麻烦。

现在的口径：

- 脚本按 `py -3` → `python` → `python3` 的顺序**实测**，选第一个"能跑且依赖齐全"的
  （不是 `where python` 取第一个 —— 能找到一个 Python 和这个 Python 能跑本系统是两回事）
- 不自动装依赖。缺依赖时打印**缺哪几个包**，并给出可直接复制粘贴的安装命令
- 依赖自检交给 `backend/check_env.py`（只用标准库，任何 Python 都能跑它）
- 依赖只需要**手动装一次**：`python -m pip install -r backend\requirements.txt`

---

## 四、目录结构

```
IT_snipe/
├── start.bat                 ★ 双击启动（用本机 Python，不建 venv，不自动装依赖）
├── stop.bat                  双击停止服务（按端口找 python.exe，不误伤别的程序）
├── backup.bat                双击备份数据库（带日期时间，自动保留最近 30 份）
├── dev.bat                   开发模式（后端热重载 + 前端 Vite dev server）
├── README.md                 面向使用者的完整说明
├── CURRENT_STATE.md          ← 本文件：项目状态唯一入口
├── CHANGELOG.md              版本变更记录
├── .gitignore
├── backups/                  备份文件（不入库）
├── .tmp/                     运行期临时文件，如 last-port.txt（不入库）
├── backend/
│   ├── run.py                启动入口
│   ├── check_env.py          依赖自检（标准库 only，给 .bat 探测用）
│   ├── backup_db.py          备份脚本（SQLite 在线备份接口 + 完整性校验）
│   ├── requirements.txt      后端依赖清单
│   ├── data/it_assets.db     SQLite 数据库（首次启动自动创建 + 灌种子数据）
│   └── app/
│       ├── main.py           应用装配 + 静态托管 + 审计中间件
│       ├── config.py         端口 / 局域网 IP / 扫码基准地址 / 备份目录 / 版本号
│       ├── database.py       引擎、会话、老库迁移（ensure_schema）
│       ├── models.py         七张表的模型定义
│       ├── schemas.py        请求响应模型
│       ├── seed.py           种子数据 + 老库回填
│       ├── services/         业务逻辑（路由只管收发）
│       │   ├── events.py         变更事件 + 字段差异比较
│       │   ├── pairing.py        配对绑定 / 解绑 / 换绑
│       │   ├── serialize.py      模型 → 响应对象的统一出口
│       │   ├── filters.py        列表筛选与盘点范围共用的条件构造
│       │   ├── audit.py          审计写入（同事务 / 独立会话两条路径）
│       │   ├── spreadsheet.py    CSV / xlsx 导出与解析公共件
│       │   └── importer.py       批量导入：列定义 / 模板 / 预览 / 落库
│       └── routers/
│           ├── assets.py         资产 CRUD + 时间线 + 状态流转
│           ├── relations.py      配对管理
│           ├── inventory.py      盘点任务 / 标记 / 差异 / 导出
│           ├── device_types.py   设备类型管理
│           ├── transfer.py       批量导入 / 导出 / 模板（/api/transfer）
│           ├── audit.py          审计查询与导出（**只有 GET**）
│           ├── meta.py           筛选候选值 / 配对统计
│           └── system.py         系统信息 + 二维码出图
└── frontend/
    ├── vite.config.ts
    └── src/
        ├── App.vue            外壳：顶栏导航 + 设置弹窗（操作人 / 扫码地址 / 备份目录）
        ├── api/index.ts       接口封装
        ├── stores/app.ts      系统信息 & 扫码地址 & 当前操作人
        ├── utils/format.ts    图标 / 状态色 / 审计配色 / 日期格式化
        ├── components/        资产表单、类型管理、状态流转、配对面板、时间线、盘点条
        └── views/             台账列表、设备详情、配对管理、盘点列表/详情、
                               批量导入、操作审计、标签打印、系统设置
```

---

## 五、数据模型

七张表 + 一张迁移记账表：

| 表 | 存什么 |
| --- | --- |
| `device_types` | 设备类型 + 类别（`host` / `display` / `other`，决定能不能参与配对） |
| `assets` | 设备台账 |
| `asset_relations` | 主机 ↔ 显示器配对关系。**解绑不删行**，改成 `active = false` 并记录解绑人和时间 |
| `asset_events` | 变更事件 —— **设备视角**的时间线 |
| `inventory_tasks` | 盘点任务（范围、发起人、状态、结束时间） |
| `inventory_items` | 盘点任务里的每台设备 + 结果。带发起时的账面快照 |
| `audit_logs` | 操作审计 —— **操作者视角**。一次写操作一条，带操作人 / IP / 类型 / 对象 / 摘要 / 成败 / 明细 |
| `schema_meta` | 一次性迁移的记账表（key/value），只用来记「某个迁移已经做过了」 |

### 关于 `audit_logs` 和 `asset_events` 的分工

这是本项目最容易搞混的一处，**两张表不能合并**：

| | `asset_events` | `audit_logs` |
| --- | --- | --- |
| 视角 | 设备（挂在某台设备下，给那台设备的时间线看） | 操作者（一次用户操作一条） |
| 触发 | 设备自身发生了变化 | 有人做了一次写操作 |
| 失败操作 | 不记 | **记** |
| 批量导入 30 台 | 30 条（每台各一条变更） | **1 条**（"导入"这一件事） |

合并成一张表，两个视角都会瘸：要么时间线被"导入"这种批量操作刷屏，要么审计丢掉"谁试过但没干成"。

`audit_logs` 刻意**不挂外键**（`target_id` 就是个裸数字），也不跟随级联删除 —— 资产被删掉了，
「谁删的」这条记录还得在，不然审计就是自欺欺人。

---

## 六、工程红线（不可违背）

**1. 「改状态」和「记历史」必须在同一个事务里。**
所有变更走 `services/events.py` 的 `record_event()`，它只 `add` 不 `commit`，由调用方连同资产改动一起提交。
审计同理：`record_audit()` 也只 `add`。这样不会出现「状态改了但历史没记」的半截数据 —— 业务回滚，审计跟着回滚。

**2. 派生状态算出来，不落库。**
「这台主机挂了几台显示器」「盘点进度百分比」都是查询时现算的，不存表。一份数据两处存，迟早对不上。

**3. 时间一律存本地时间。**
`server_default=func.now()` 在 SQLite 下编译成 `CURRENT_TIMESTAMP`，**那是 UTC** —— 东八区机器上
下午 5 点录入的设备，库里写着上午 9 点。已全部改为 `datetime.now()`，老库做了一次性平移（见下）。

**4. 审计日志只读。**
`routers/audit.py` 里只有 3 个 GET 端点，**没有任何写接口**。不提供编辑，也不提供删除。

**5. 不做登录体系。**
「操作人」由用户在设置里手填（可留空），靠 IP + 手填操作人区分。**不要自作主张加登录。**

---

## 七、关键设计决策（为什么这么做）

> 这一节记录"看起来可以那样做、但我们选这样做"的地方。改代码前先看这里，
> 否则很容易把有意的设计当成 bug 改掉。

| # | 决策 | 理由 / 代价 |
| --- | --- | --- |
| 1 | 审计和变更历史是**两张表** | 见「五、数据模型」的分工表。合并会让两个视角都残 |
| 2 | **失败的请求也写审计**，走独立会话 | 反复尝试删别人设备、非法状态被 409 挡下 —— 这些在业务表里毫无痕迹，恰是审计最值钱的信号。走独立会话是因为业务事务已经炸了，挂在上面写必然被回滚 |
| 3 | 审计 IP **默认不信任 `X-Forwarded-For`** | 那个头能伪造，能自己填的 IP 让审计毫无意义。只有设 `IT_ASSET_TRUST_PROXY=1` 才读 |
| 4 | IP 用 **ContextVar** 传，不给每个 handler 加 `request` 参数 | 一个值要被十几个路由用；ContextVar 按异步任务隔离，不是全局变量，并发不串号 |
| 5 | **导入导出模板共用同一份列定义**（`ASSET_COLUMNS`） | 绝不出现"导出的列导不回来"。模板 / 导出 / 导入校验三处同源 |
| 6 | **预览和落库跑同一段代码**（`build_plan()`） | 不存在"预览说没事、确认就报错"。不用服务端暂存 token 那套（token 过期、文件换了、两人互相覆盖） |
| 7 | 更新已有设备时，**空单元格 = 不修改**，不是清空 | 批量改数据最常见的是"拿两列的表把 30 台机器的使用人刷一遍"。若空=清空，一次就抹掉品牌型号 SN 位置日期且不可撤销。想清空写 `-` |
| 8 | 导入结果分**新增 / 更新 / 无变化 / 失败**四桶 | 少了「无变化」，同一文件导第二遍会谎报"更新了 10 条"，用户再也无法判断导入是否生效 |
| 9 | 导入报错给**Excel 真实行号** | 告诉用户"第 7 行有问题"他才能直接定位；给内部索引 0 等于没说 |
| 10 | CSV 编码三级兜底 `utf-8-sig` → `gbk` → `latin-1` | 中文版 Excel「另存为 CSV」默认是 GBK，不认这个用户存一遍再传就全乱码 |
| 11 | `/api/transfer` **独立前缀** | `/api/assets/{asset_id}` 是路径参数路由，`/api/assets/export` 会被当成 `asset_id="export"` 抢先匹配。靠调整路由顺序躲坑意味着以后每加一条都要重想 |
| 12 | 导出中文文件名走 **RFC 5987** `filename*=UTF-8''` | HTTP 头是 latin-1，中文必须先 `quote`；同时留 ASCII `filename=` 给老浏览器兜底 |
| 13 | 配对**解绑不删行**，改 `active=false` | 换绑轨迹才留得住 |
| 14 | 启动脚本**不建虚拟环境、不自动装依赖** | 见「三、运行环境约定」。在"本机 Python 本来就能跑"的机器上，自动建环境反而会让它启动不了 |
| 15 | 启动脚本**实测** Python 而不是取 PATH 第一个 | 电脑上可能有好几个 Python，PATH 排第一的未必是装了依赖的那个。`py -3` 优先是因为它是官方启动器，不受 PATH 顺序影响 |
| 16 | 三个 `.bat` 一律 **GBK + CRLF、无 BOM** | Windows 控制台默认代码页 936。存成 UTF-8 中文提示会变乱码，配 `chcp 65001` 又会错位 |
| 17 | 时区校正**只平移** `created_at` / `updated_at` | `released_at` / `closed_at` / `checked_at` 本来就是本地时间，往后挪 8 小时等于把好数据改坏 |
| 18 | 一次性迁移用 `schema_meta` **记账**，幂等 | 重复启动不会重复执行 |

---

## 八、功能现状

三个阶段已全部交付。每阶段的功能点如下（详细操作说明见 README 对应章节）。

### 第一阶段：资产台账 + 二维码扫码查看

- 资产 CRUD、筛选（关键字 / 类型 / 状态 / 使用人 / 位置 / 是否配对）、分页
- 每台设备生成二维码，手机扫码直达详情页
- 设备类型管理（含 `category` 主机/显示器/其他，决定能否配对）
- 变更历史时间线（录入 / 信息变更 / 状态流转 / 配对 / 盘点都记）
- 批量打印二维码标签（A4 排版）

### 第二阶段：主机-显示器配对 + 状态流转 + 盘点

- 配对看板：主机及其显示器、待配对池、统计
- 绑定 / 换绑 / 解绑（解绑保留记录），配对记录可查
- 状态流转：领用 / 归还 / 送修 / 修好 / 报废 / 恢复，非法流转返回 409
- 盘点任务：发起（范围复用资产筛选口径）、逐台标记（已盘/未盘/异常）、差异报表、结束/重开/删除
- 扫码进详情页顶部的盘点快捷条
- 盘点结果导出 xlsx / csv

### 第三阶段：操作审计 + 批量导入导出 + Windows 启动脚本

- **操作审计**：18 种操作类型全覆盖（新增/编辑/删除/状态流转/配对/解绑/换绑/盘点/导入/导出/类型管理）
  - 审计页支持按时间、操作类型（分组多选）、操作人、IP、结果、关键字筛选
  - 时间线式只读展示，可跳转到对应设备 / 盘点任务
  - 导出 xlsx / csv（8 列：时间/操作人/IP/操作类型/操作对象/操作内容/结果/补充信息）
  - **失败请求也留痕**，摘要翻译成人话
- **批量导入**：模板下载（xlsx 三工作表 / csv）→ 拖入文件 → 预览（新增/更新/无变化/失败四桶 + 逐行问题）→ 确认入库
  - 两种模式：仅新增（重复即报错）/ 新增或更新（按编号匹配）
  - 设备类型不存在时可选择自动创建或报错
  - 失败原因逐条列出，带 Excel 真实行号
- **批量导出**：全部或按当前筛选；可选附带配对显示器信息；列与导入模板完全一致
- **Windows 脚本**：`start.bat` / `stop.bat` / `backup.bat` / `dev.bat`

---

## 九、怎么跑起来

### 前置：装一次依赖

需要 Python 3.10+。依赖只需要装一次：

```bat
python -m pip install -r backend\requirements.txt
```

国内网络慢就加镜像：

```bat
python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r backend\requirements.txt
```

不知道用哪个 Python？先跑一下自检，它会告诉你当前解释器缺什么：

```bat
python backend\check_env.py
```

### 日常启动：双击 `start.bat`

脚本做的事：检查是否已在运行 → 挑一个可用端口（8080 起，被占就顺延）→ 找可用的 Python
→ 检查依赖 → 前端页面（`frontend/dist` 存在就跳过构建）→ 启动服务 → 5 秒后自动开浏览器。

- 停止：本窗口 `Ctrl+C`，或另开窗口双击 `stop.bat`
- 改了前端代码要重新构建：`start.bat --rebuild`
- 指定端口：`set IT_ASSET_PORT=18080` 后再运行（指定了就不再顺延）
- **缺依赖时不会自动装**，会打印缺哪几个包和一行可直接复制的安装命令，装完再双击即可

### 手动启动（不用脚本）

```bat
cd backend
python run.py                    :: 默认 8080，可从任意目录执行
python run.py --port 18080
```

前端只在开发时需要构建：

```bat
cd frontend
npm install
npm run build                    :: 产物交给后端托管
```

开发时用 `dev.bat`，或 `npm run dev`（Vite 把 `/api` 代理到 `127.0.0.1:8080`）。

### 局域网 / 手机访问

1. 手机和电脑连同一个 WiFi
2. 看启动窗口里 `手机扫码 : http://192.168.x.x:8080` 那一行
3. 首次需要放行防火墙（管理员权限执行一次）：

```bat
netsh advfirewall firewall add rule name="IT资产管理8080" dir=in action=allow protocol=TCP localport=8080
```

探测到的 IP 不对（有 VPN / 虚拟机 / WSL 时常见）：点右上角 ⚙ 设置，把「自定义扫码地址」填成
`http://192.168.x.x:8080` 即可，二维码会立刻按新地址重新生成。

### 备份

双击 `backup.bat` → 在 `backups\` 生成 `it_assets_20260924_153012.db`，自动只留最近 30 份。
它走 SQLite 的**在线备份接口**，服务正在跑、有人正在写的时候也能拿到完整一致的数据，比直接复制文件安全。

用本机 Python 直接跑也可以：

```bat
python backend\backup_db.py --keep 30
python backend\backup_db.py --list          :: 只看不建
python backend\backup_db.py --out D:\bak    :: 换目录
```

---

## 十、验收

### 自动化验收（可重复执行）

| 脚本 | 覆盖 | 结果 |
| --- | --- | --- |
| `phase3_api_test.py` | 后端 16 个 section / 132 项 | **132 / 132 通过** |
| `e2e-phase3.js` | 真实 Chromium 浏览器端到端 79 项 | **79 / 79 通过** |
| `vue-tsc --noEmit` | 前端类型检查 | 通过 |
| `vite build` | 前端构建 | 通过 |

> 这两个验收脚本是开发期工具，放在仓库之外的本地测试目录，**未随项目发布**。
> 需要复现时按 README「验收步骤」一节手工走一遍即可。
>
> 它们每次运行前会把数据库复位成 10 台种子资产并清空审计记录 —— 这是为了让断言
> 有确定的起点。所以**验收脚本千万不要对着正在使用的数据跑**。

### 手工验收（脚本覆盖不到的）

1. **双击 `start.bat`** —— 能不能顺利起来、浏览器自动打开
2. **双击 `backup.bat`** —— `backups\` 里是否出现带日期时间的文件
3. **手机连同一局域网扫码** —— 能否打开并看到设备详情
4. **审计页**：改一台设备的状态 → 审计页应出现「时间 + IP + 操作 + 对象 + 变更内容」
5. **导入闭环**：下载模板 → 填 10 台 → 导入预览显示 10 条新增 → 确认入库；
   同一文件再导一次，选「仅新增」报重复错误、选「新增或更新」报无变化
6. **导出闭环**：导出当前筛选结果 → 能正常打开且列齐全 → 改一改能再导回来

README「验收步骤」一节按三个阶段给出了更细的逐条清单。

---

## 十一、接口一览

服务起来后打开 `http://127.0.0.1:8080/docs` 有交互式文档。按前缀分组：

| 前缀 | 覆盖 | 备注 |
| --- | --- | --- |
| `/api/assets` | 台账 CRUD、时间线、配对记录、状态流转 | 状态流转非法返回 409 |
| `/api/pairings` | 配对看板、绑定、换绑、解绑、记录 | 解绑不删行 |
| `/api/inventory` | 盘点任务、标记、差异、导出、扫码上下文 | 范围与资产列表同一套筛选口径 |
| `/api/device-types` | 设备类型增删改查 | 有设备在用则拒绝删除 |
| `/api/transfer` | 模板下载、批量导入、批量导出 | 独立前缀，避开 `/assets/{id}` 抢先匹配 |
| `/api/audit` | 审计列表、筛选项、导出 | **只有 GET，无写接口** |
| `/api/meta/filters` | 筛选候选值 + 配对统计 | 使用人/位置现算，不建字典表 |
| `/api/qrcode.png` · `/api/system/info` · `/api/health` | 二维码出图、系统信息、健康检查 | — |

完整的参数级接口表见 README「接口一览」。

---

## 十二、已知限制

- **不能用手机摄像头实时扫码**。浏览器要求 HTTPS 或 localhost 才能调摄像头，而本系统是局域网
  `http://192.168.x.x`。改用系统相机 / 微信扫已经贴好的二维码 —— 效果一样。
- **没有登录和权限**。这是有意的（见红线 5），「操作人」靠手填 + IP 区分，不能作为强身份证明。
- **没有附件**（发票、保修卡照片），没有保修到期提醒。
- **审计日志不能删**。数据量大时只能靠筛选和分页，没有归档/清理功能。
- **导入是同步的**。超大文件（上万行）会让请求变慢，目前没有后台任务队列。
- **`frontend/dist` 需要 Node 才能构建**。把 dist 一起拷过去，目标机器不装 Node 也能跑。

---

## 十三、下一步（尚未实现）

按价值排序：

1. **附件**：发票、保修卡照片，挂在设备下
2. **保修到期提醒**：`warranty_until` 字段已经在库里了，差一个提醒视图
3. **登录与权限**（如果确实需要 —— 但会打破「不做登录」的当前设计）
4. **审计归档**：按时间把老审计导出后清理

明确**不要做**的：采购、合同、财务、工单、审批流（立项约束）。

---

## 十四、维护约定

改代码后按这个顺序自查，别跳步：

1. `python backend\check_env.py` —— 环境没坏
2. `cd frontend && npx vue-tsc --noEmit` —— 类型干净
3. `npm run build` —— 前端能构建
4. 复位数据库后跑 `phase3_api_test.py` 与 `e2e-phase3.js` —— 自动化验收全绿

容易踩的坑：

- **改了后端依赖要同步改 `backend/check_env.py` 的清单**。`requirements.txt` 管"装什么"，
  `check_env.py` 管"装完能不能 import"，两边必须一起动。
- **改 `.bat` 必须存成 GBK + CRLF**，中文提示才不会乱码。
- **加了新路由要注意路径参数路由的顺序问题**，批量的走 `/api/transfer` 这类独立前缀。
- **改了行为就更新本文件**。这是项目的状态基准，过期了比没有更糟。

---

*最后更新：2026-09-24 · 对应版本 v1.0.0*
