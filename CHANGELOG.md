# 更新日志

本项目的所有重要变更都记在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

当前基线：**v1.0.2**（2026-09-24）

---

## [未发布]

### 计划中

- 附件：发票、保修卡照片，挂在设备下
- 保修到期提醒（`warranty_until` 字段已在库中）
- 审计归档：按时间导出后清理
- 登录与权限（如果确实需要 —— 会打破当前「不做登录」的设计）

---

## [1.0.2] - 2026-09-24

一条后台批量配对命令 + 四处筛选体验优化 + 一处手机端老问题修复。没有数据库结构变更，
启动时也不会碰任何业务数据 —— 直接覆盖代码、重启即可。

### 新增

- **新增 `backend/link_monitors.py` 命令**：按编号规则批量补「主机 ←→ 显示器」配对。
  批量导入的资产里，显示器编号常常是「对应主机编号 + `-M`」（如 `B022-03-00001-M` ↔ `B022-03-00001`），
  该命令按这个命名规则一次性关联完，不用在界面上几十上百台逐个点「绑定」。
  - **默认只分析不写库**，必须加 `--yes` 才落库；落库前自动备份，备份失败即中止
  - 写入复用 `services/pairing.py::bind()`，校验 / 状态机 / `asset_events` 时间线与界面点按钮完全一致；
    审计按「一次批量操作记一条」的口径写入，明细放结构化 `detail`
  - **只做加法**：已挂在别的主机上的显示器跳过并列出来（不擅自换绑，换绑会抹掉归属历史）；
    已配好的重复跑会落到「无需重复绑定」分支，不产生重复数据
  - 任何一条失败整批回滚；`--suffix` / `--operator` / `--no-backup` 可调
  - 实测：46 台 `-M` 显示器一次配对成功；重跑第二遍零写入

### 优化

- **资产台账：设备类型 / 存放位置 / 使用人三个筛选框改成多选**。原来是单选，一次只能看一个类型、
  一个位置，想看「主机和笔记本各有多少」得来回切。多选后可以一次勾几个，筛选框里以标签展示已选项，
  超过一个自动折叠成 `+N`（悬停能看到全部）。计数、重置、导出链接全部跟着新口径走。
  - 「状态」**故意保持单选** —— 上面那排统计卡点一下就切状态，卡片是单选语义，做成多选会和卡片对不上
- **配对管理：给主机加显示器时，默认只列还没配出去的显示器**。以前候选列表里混着一堆
  「当前挂在 XXX（换绑）」，要挑一台新显示器得先做完排除法。现在默认把这些藏起来，弹窗里
  另加了搜索框和「显示已挂机的（N 台）」开关；真要换绑就勾上开关，行为不变（仍然一条事务做完解绑+绑定）
- **配对管理：主机列新增「只看没配显示器的（N）」开关**。这一栏原来是刷完整个主机列表自己找
  哪些主机还空着，现在一个勾就能只看这 21 台。注意待配对显示器右侧的「挂到主机」下拉**不受这个开关影响** ——
  它是选目标主机用的，收窄了反而选不到想要的那台
- **标签打印：左侧选择面板新增「设备类型」「存放位置」筛选**。补打一台设备的标签时，
  原来是搜索框里猜关键字、或者在全量列表里翻；现在可以直接「显示器 + 质量管理部」两步缩到几台。
  筛选行会提示「筛出 N 台」并提供一键清空；全选 / 反选只作用于筛选后的可见列表

### 修复

- **手机视口下资产台账会被分页撑出横向滚动条**。分页组件排了「上一页 + 页码 + 下一页 + 跳页输入框」
  四段，窄屏塞不下，实测整条宽 446px（390px 的屏溢出 28px）。手机上隐藏跳页输入框并收窄页码按钮，
  剩下三段放得下（页码多了 Element Plus 会自动折叠成省略号）。
- **标签打印的位置筛选框刚打开时是全选状态**（开发本版时发现的）。候选项的 ref 取名成 `locations`，
  与接口返回的 `locations` 字段撞了，`locations.value = res.locations` 顺手把 14 个候选位置全变成了
  "已选中"。**而且因为"全选所有位置"恰好等价于不筛，数量断言完全看不出来** —— 是靠人工看截图里
  「会议室-人事行政部 × +13」这个异常的折叠标签才发现的。候选项改名 `locationOptions` 后修复，
  并补了两条断言专门盯这个：刚打开时不许有已选标签、不许出现「筛出 N 台」提示。

### 变更

- **后端 `build_conditions` 的 `user_name` / `location` 从「单值精确匹配」改为「支持逗号分隔多值」**
  （`services/filters.py`）。单值传参行为与改动前逐项一致（已对全部位置 / 使用人抽样回归）。
  这两个条件是和「发起盘点」的范围共用一份的，所以盘点范围也能跟着用多值，不存在两处口径打架。

### 验收

- 真实浏览器端到端 **25 / 25 通过**（临时 8099 实例，只读操作，未改动任何业务数据）：
  台账多选逐项与接口计数对账、配对页两处开关、弹窗默认隐藏与开关放开、标签页类型筛选与清空、
  标签页初始态无预选回归、390×844 手机视口无横向溢出、控制台无 JS 报错。
- 接口侧回归：位置 / 使用人单值语义与 SQL 直查逐项一致；`vue-tsc --noEmit` 零错误；`vite build` 通过。
- 数据库仍是 142 台资产、46 对有效配对，与升级前一致。

---

## [1.0.1] - 2026-09-24

文档收缩版本：把"项目状态"和"使用者说明"两份文档各自定位清楚，删掉两份文档之间互相重复的内容。
代码行为只有一处变更（启动不再自动灌演示数据），其余为文档重构与版本号统一。

### 变更 — 启动不再自动灌演示数据

**问题**：启动时 `lifespan` 会调 `seed_if_empty()`，空库就往里灌 6 个设备类型 + 10 台示例设备。
删掉 `it_assets.db` 重启，这些数据又回来了 —— 分不清"库里这些设备是我录的还是系统塞的"，
误删数据库文件后还会凭空"长"出十台假设备跟真实数据混在一起。

- **新增配置项 `IT_ASSET_SEED_ON_STARTUP`，默认 `false`**（`backend/app/config.py`）。
  启动只建表 + 补字典，不写任何业务数据
- **`seed.py` 拆成两条路径**：
  - `ensure_dictionary(db)` —— 设备类型字典。**启动时自动调用**，不含任何资产
  - `seed_demo(db)` —— 10 台示例资产 + 配对 + 初始履历。**只在显式执行时才跑**
  - 原 `seed_if_empty()` 已移除，其行为由 `seed_demo()` 承接
- **新增 `backend/seed.py` 命令**：手动灌演示数据，`--check` 只看各表条数
- **新增 `backend/reset.py` 命令**：清空全部业务数据（资产 / 配对 / 履历 / 盘点 / 审计日志），
  **保留表结构和设备类型字典**。两道保险：先自动备份到 `backups\`，再要你亲手输入 `RESET` 确认；
  输错、没输入、stdin 为空一律中止。支持 `--check` / `--yes` / `--no-backup`
- **`database.py` 新增表分类**：`BUSINESS_TABLES`（reset 清空）与 `DICTIONARY_TABLES`（自动初始化、
  不被清空），删除顺序按外键依赖排列（子表优先），另加各表行数统计 `table_counts()`
- **启动横幅**：空库时明确提示「当前为空库（设备类型 6 个，资产 0 台）」并给出两个维护命令

**改变后的行为**：删掉 `backend\data\it_assets.db` 重启 = 一个空库，设备类型在、资产 0 台。
要演示数据就 `python backend\seed.py`。

### 变更 — 文档收缩为两份，各自定位清楚

**问题**：`CURRENT_STATE.md`（450+ 行）与 `README.md`（790+ 行）互相重复 ——
七张表、工程红线、设计取舍、验收步骤、接口一览两份都写了一遍，改一处要改两遍，迟早不一致。

- **`CURRENT_STATE.md` 重排为十四节固定结构**，目标是「读这一份就够改代码」：
  约束 → 系统干什么 → 技术栈与运行环境 → 目录结构 → 数据模型 → 工程红线 → 关键设计决策 →
  功能现状 → 怎么跑 → 验收 → 接口一览 → 已知限制 → 下一步 → 维护约定
- **`CURRENT_STATE.md` 去掉与 README 重叠的操作细节**：怎么装、端口怎么挑、防火墙怎么放行、
  局域网排错、逐条验收步骤，全部只留一行跳转 README
- **`CURRENT_STATE.md` 的关键设计决策由 23 条合并为 15 条**（只合并重复主题：IP 处理、中文编码、
  启动脚本口径、一次性迁移、导入反馈、reset 安全阀），**没有删除任何一条决策的实质内容**。
  其余不需压缩的三处刻意保持原样：项目约束原文（逐字）、工程红线（七条全留）、
  `audit_logs` vs `asset_events` 分工表（最容易改错的地方）
- **`README.md` 回归使用者视角**，删掉五节与 CS 重复的内容：
  技术栈 / 目录结构 / 七张表 / 三条工程红线 / 老库升级 / 设计取舍 / 验收步骤整节 / 接口参数表
- **`README.md` 把「第 N 阶段做了什么」改成「现在有什么」**，去掉 changelog 语气
- 两份文档净效果：CS 515 → 347 行（-33%），README 797 → 306 行（-62%），
  两份加起来比原来少，且**没有一处内容需要维护两份**

### 变更 — 版本号统一为 1.0.1

`backend/app/config.py`（`APP_VERSION`）、`backend/app/__init__.py`（`__version__`）、
`frontend/package.json`、`frontend/package-lock.json` 四处统一。

### 验收

| 范围 | 项数 | 结果 |
| --- | --- | --- |
| 后端 API（`phase3_api_test.py`，16 个 section） | 132 | 全部通过 |
| 浏览器端到端（`e2e-phase3.js`，真实 Chromium） | 79 | 全部通过 |
| `/api/health` 回显版本号 | — | `1.0.1` |
| 文档自查：两份文档无重复章节、跳转链接有效 | — | 通过 |

---

## [1.0.0] - 2026-09-24

**首个版本基线。** 三个阶段的开发成果合并定版：资产台账 + 扫码查看 → 主机显示器配对 + 状态流转 + 盘点 → 操作审计 + 批量导入导出 + Windows 启动脚本。

> **关于版本号**：开发过程中曾用 `1.1.0` / `1.2.0` 作为内部迭代编号来标记进度，从未对外发布。
> 定版时统一收敛到 `v1.0.0`，内部编号不再使用。全部内容见下面的 Added 部分。

### 新增 — 第一阶段：资产台账 + 二维码扫码查看

- **资产台账**：新增 / 编辑 / 删除 / 详情，支持按关键字、设备类型、状态、使用人、存放位置、是否配对筛选，分页
- **二维码**：每台设备生成二维码 PNG，贴在机器上，手机扫码直达该设备详情页
- **设备类型管理**：类型可自定义，另存 `category`（主机 / 显示器 / 其他）决定能否参与配对 —— 类型名字随便改，配对能力不受影响
- **变更历史时间线**：录入 / 信息变更 / 状态流转 / 配对解绑 / 盘点标记都记一条，每台设备一条自己的历史
- **标签批量打印**：A4 排版批量出二维码标签

### 新增 — 第二阶段：主机-显示器配对 + 状态流转 + 盘点

- **配对管理**：看板视图（主机及其显示器 + 待配对池 + 统计）、绑定、换绑、解绑
  - 解绑**不删记录**，改为 `active = false` 并记下解绑人和时间，换绑轨迹留得住
- **状态流转**：领用 / 归还 / 送修 / 修好 / 报废 / 恢复六种操作，非法流转返回 409 并说明原因
- **盘点任务**：发起盘点（范围与资产列表同一套筛选口径）、逐台标记 已盘 / 未盘 / 异常、差异报表、结束 / 重新开启 / 删除
  - 每台设备带一份**发起时的账面快照**，之后账面被改过能看出「盘点时在哪」
- **盘点结果导出**：xlsx / csv，可只导差异、按标记结果筛选
- **扫码盘点快捷条**：手机扫设备码进详情页后，顶部直接标记，不用层层点进去

### 新增 — 第三阶段：操作审计 + 批量导入导出 + Windows 脚本

#### 操作审计

- 新增 `audit_logs` 表，覆盖 **18 种操作类型**：新增 / 编辑 / 删除 / 状态流转 / 配对 / 解绑 / 换绑 / 导入 / 导出 / 盘点四项 / 设备类型三项 / 审计导出
- 每条记录含：时间、操作人（手填，可空）、IP、操作类型、操作对象、变更内容摘要、成功/失败、结构化明细
- **失败的请求也留痕**：非法状态被 409 挡住、尝试修改不存在的设备、上传坏文件被拒 —— 这些在业务表里毫无痕迹，却是审计最有价值的信号
- 审计页面：统计卡（全部 / 今日 / 失败，点击即筛选）、按时间范围 / 操作类型（分组多选）/ 操作人 / IP / 结果 / 关键字筛选
- 时间线式只读展示，可跳转到对应的设备详情页 / 盘点任务
- 导出 xlsx / csv（8 列：时间 / 操作人 / IP / 操作类型 / 操作对象 / 操作内容 / 结果 / 补充信息）
- **审计日志只读**：表上无外键、不跟随级联删除，接口层只有 3 个 GET、没有任何写接口

#### 批量导入

- **模板下载**：xlsx（含「资产导入 / 填写说明 / 列说明」三个工作表）或 csv，可带示例数据
- **两步式流程**：拖入文件 → 预览（**不写库**）→ 确认入库。预览与落库跑的是同一段校验代码
- **两种模式**：仅新增（遇到已存在的编号就报错）/ 新增或更新（按编号匹配，存在则更新）
- **设备类型不存在**时可选「自动创建」或「报错」
- **校验规则**：IT 资产编号必填且全库唯一、必填字段校验、日期格式校验、状态别名归一化（"使用中"→ 在用、"报废"→ 已报废）
- **预览四桶计数**：新增 / 更新 / 无变化 / 失败 —— 「无变化」这一桶不能少，否则同一文件导第二遍会谎报"更新了 10 条"
- **错误逐行定位**：给 Excel 里的**真实行号**，例如「第 5 行：日期格式无法识别」
- **更新时空单元格 = 不修改**（不是清空）。想清空某字段写 `-`
- 导入操作写审计日志：一次导入一条（不是 30 台设备各一条）

#### 批量导出

- 支持导出**全部资产**或**按当前筛选条件**导出
- 可选附带配对的显示器信息
- 导出列与导入模板**完全一致**（共用同一份 `ASSET_COLUMNS`），保证「导出 → 改 → 再导入」闭环
- CSV 导出带 BOM，中文版 Excel 直接打开不乱码

#### Windows 脚本

- **`start.bat`**：双击启动。检查是否已在运行 → 挑可用端口（8080 起自动顺延）→ 找可用的 Python
  → 检查依赖 → 前端页面（`dist` 存在则跳过构建）→ 启动服务 → 5 秒后自动打开浏览器
- **`stop.bat`**：按端口找到监听进程，**校验进程名是 `python.exe` 才结束**，不会误伤占用同端口的其他程序
- **`backup.bat`**：一键备份，生成 `backups\it_assets_20260924_153012.db`，自动只保留最近 30 份
  - 走 SQLite **在线备份接口** + 完整性校验，服务运行中也拿到一致数据；没有 Python 时才退化为直接复制
- **`dev.bat`**：开发模式（后端热重载 + 前端 Vite dev server）
- 新增 `backend/check_env.py`：依赖自检（只用标准库），缺依赖时说清楚缺哪几个并给出安装命令
- 新增 `backend/backup_db.py`：备份实现

### 变更

- **版本号**：内部编号 `1.1.0` / `1.2.0` 收敛为对外基线 `1.0.0`（`backend/app/config.py`、`backend/app/__init__.py`）
- **启动脚本运行环境口径变更**：`start.bat` / `dev.bat` / `backup.bat` **不再创建 `backend\.venv`，也不再自动执行 `pip install`**，改为使用电脑上已装好的 Python
  - 按 `py -3` → `python` → `python3` 的顺序**实测**（能跑 + 依赖齐全才采用），而不是取 `where python` 的第一个 —— 能找到一个 Python 和这个 Python 能跑本系统是两回事
  - 缺依赖时打印缺失清单和一行可直接复制粘贴的安装命令，依赖只需手动装一次
  - 这样改是因为：在"本机 Python 本来就能跑"的机器上，脚本自己建环境再装包反而成了新的失败点
- **时间戳统一为本地时间**：`models.py` 中 7 处 `server_default=func.now()`（SQLite 下是 UTC）改为 `datetime.now()` / `onupdate=datetime.now`
- **资产列表导出改为服务端导出**：原来是前端拼 CSV（列与导入模板对不齐、也没有筛选外的一致性保障），现在走 `/api/transfer/export`，与导入模板共用列定义，并支持"当前筛选 / 全部"和"附带配对信息"
- **导出/导入统一走 `services/spreadsheet.py`**：表头底色、加粗、冻结行、CSV BOM、中文文件名编码只实现一次，避免各导出模块各写一套导致外观和口径不一致
- **`/api/transfer` 使用独立路由前缀**：避开 `/api/assets/{asset_id}` 对 `/api/assets/export` 的抢先匹配
- **`ensure_schema()` 返回值由 `None` 改为 `dict`**：启动日志回显这次实际做了哪些迁移
- **中文文件名走 RFC 5987**：`filename*=UTF-8''` + ASCII `filename=` 兜底

### 修复

- **`openpyxl` 缺失于 `requirements.txt`**（第二阶段起一直靠"恰好装在环境里"跑通，惰性 import 掩盖了问题）—— 已补进依赖清单。这条正是「新电脑双击 start.bat」这条验收标准要抓的 bug
- **SQLite 时间戳 UTC / 本地时间混用**：东八区机器上写入的时间比实际早 8 小时。已修默认值，并对老库做一次性平移 —— 只平移确定被写成 UTC 的 `created_at` / `updated_at`，**绝不动** `released_at` / `closed_at` / `checked_at`（那几个本来就是本地时间，挪 8 小时等于把好数据改坏）。用 `schema_meta` 记账保证幂等
- **axios 1.20 起 `params` 必须是对象**：传拼好的查询串会在库内部抛 `target must be an object`，请求根本没发出去、后端日志零痕迹，表现是"点了按钮没反应"。拆成 `queryString()`（拼下载链接）与 `pruneParams()`（交给 axios）两个函数
- **`dev.bat` 的工作目录问题**：原先在项目根目录执行 `run.py`，而该文件在 `backend/` 下，实际起不来。改为先切换到 `backend` 再启动
- **`backend\.venv` 依赖不完整导致启动失败**：去掉虚拟环境（见上方「变更」）

### 工程约定

- 新增 `CURRENT_STATE.md` 作为项目状态唯一入口，原 `《每阶段总约束.txt》` 逐字并入其中并删除原文件
- `.gitignore` 补齐：`backend/data/`、`backups/`、`.tmp/`、`.venv/`、`frontend/node_modules/`、`frontend/dist/` 均不入库

### 验收

| 范围 | 项数 | 结果 |
| --- | --- | --- |
| 后端 API（`phase3_api_test.py`，16 个 section） | 132 | 全部通过 |
| 浏览器端到端（`e2e-phase3.js`，真实 Chromium） | 79 | 全部通过 |
| 前端类型检查（`vue-tsc --noEmit`） | — | 通过 |
| 前端构建（`vite build`） | — | 通过 |

---

## 版本对照

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| 1.0.2 | 2026-09-24 | 新增 `link_monitors.py` 批量配对命令；台账筛选改多选；配对页加未配对主机筛选、加显示器弹窗默认隐藏已挂机的；标签打印加类型 / 位置筛选；修手机端分页溢出 |
| 1.0.1 | 2026-09-24 | 启动不再自动灌演示数据（新增 `seed` / `reset` 命令）；文档收缩为两份；版本号统一 |
| 1.0.0 | 2026-09-24 | **首个对外基线**，含三个阶段的全部功能 |
| ~~1.2.0~~ | — | 开发期内部编号，未发布，内容已并入 1.0.0 |
| ~~1.1.0~~ | — | 开发期内部编号，未发布，内容已并入 1.0.0 |
